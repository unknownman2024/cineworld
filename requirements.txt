# Both packages ship a module called `cloudscraper`; installing the base
# first and enhanced second lets the fork's files win the name collision.
cloudscraper==1.2.71
cloudscraper-enhanced
requests
brotli
tzdata
